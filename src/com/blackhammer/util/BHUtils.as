package com.blackhammer.util {		import flash.display.*;	import flash.geom.*;	import flash.media.*;	import flash.events.*;		public class BHUtils extends Sprite{			public function BHUtils():void{				}				public function ShowChildren(dispObj:*):void {			for(var i:int = 0; i < dispObj.numChildren; i++) {				var obj:DisplayObject = dispObj.getChildAt(i);				if (obj is DisplayObjectContainer) {					trace(obj.name, obj);					ShowChildren(obj);				} else {					trace(obj);				}			}		}			public function ShowChildren2(dispObj:DisplayObject, indentLevel:Number):void {		for (var i:uint = 0; i < dispObj.numChildren; i++) {			var obj:DisplayObject = dispObj.getChildAt(i);			if (obj is DisplayObjectContainer) {				trace(padIndent(indentLevel), obj.name, obj);				ShowChildren(obj, indentLevel + 1);			} else {				trace(padIndent(indentLevel) + obj);			}		}	}		private function padIndent(indents:int):String {		var indent:String = "";		for (var i:uint = 0; i < indents; i++) {			indent += "    ";		}		return indent;	}										}}